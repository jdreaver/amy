List a = Nil | Cons { car :: a, cdr :: List a }

main :: Int
main = 1

g :: forall a. a -> { x :: a }
g x = { x = x }

h :: forall a. a -> List a
h x = Cons { car = x, cdr = Cons { car = x, cdr = Nil } }
